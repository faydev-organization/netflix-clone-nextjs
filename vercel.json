{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "build": {
    "env": {
      "DATABASE_URL": "@database-url"
    },
    "build": "prisma generate && next build"
  }
}
